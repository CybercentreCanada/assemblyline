name: root-build

schedules:
- cron: "0 3 * * *"
  displayName: Daily build
  branches:
    include:
    - master
  always: true
trigger: none
pr: none

pool:
  vmImage: "ubuntu-20.04"

extends:
  template: templates/stages/build-nightly.yaml
  parameters:
    build_type: "stable"
